# Rockpool production-like local profile
# Runs with pre-built client, no file watchers, LAN-accessible via Caddy on 0.0.0.0.
# See doc/EDD/021_Production_Profile.md for design details.

# Environment
NODE_ENV=development

# Server (Express control plane, behind Caddy)
PORT=10163
SRV0_PORT=10080
SRV1_PORT=10081
DB_PATH=rockpool-production.db
SPA_ROOT=packages/client/dist

# Runtime: auto-detected from platform (darwin=tart, linux=firecracker)
FIRECRACKER_BASE_PATH=.firecracker
SSH_KEY_PATH=images/ssh/rockpool_ed25519

# Caddy
CADDY_ADMIN_URL=http://localhost:10019
CADDY_USERNAME=admin
CADDY_PASSWORD=admin

# Queue (ElasticMQ on production port)
QUEUE_ENDPOINT=http://localhost:10324
QUEUE_URL=http://localhost:10324/000000000000/workspace-jobs

# Helper URLs (resolved through Caddy)
DASHBOARD_URL=http://localhost:10080
API_URL=http://localhost:10080/api
