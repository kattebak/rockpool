# E2E test environment — real stack, separate ports, ephemeral DB
# Loaded by npm scripts via: node --env-file=test.env
# Also loaded by server/worker via: --env-file=test.env (in ecosystem.test.config.cjs)

# Server
PORT=9163
SRV0_PORT=9080
SRV1_PORT=9081
DB_PATH=/tmp/rockpool-e2e.db
SPA_ROOT=packages/client/dist

# Caddy
CADDY_ADMIN_URL=http://localhost:9019
CADDY_USERNAME=test
CADDY_PASSWORD=test

# Queue (ElasticMQ on test port)
QUEUE_ENDPOINT=http://localhost:9424
QUEUE_URL=http://localhost:9424/000000000000/workspace-jobs

# E2E helper URLs (resolved through Caddy, not direct)
DASHBOARD_URL=http://localhost:9080
API_URL=http://localhost:9080/api
