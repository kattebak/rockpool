# Rockpool local development environment
# Copy to development.env and fill in your secrets:
#   cp development.env.example development.env
#
# See doc/EDD/003_Caddy_Reverse_Proxy.md appendix for GitHub OAuth setup.

# Environment
NODE_ENV=development

# Runtime: "tart" (macOS), "firecracker" (Linux), "stub" (tests)
# Auto-detected from platform if not set
RUNTIME=tart
SPA_PROXY_URL=http://localhost:5173
SSH_KEY_PATH=images/ssh/rockpool_ed25519
# FIRECRACKER_BASE_PATH=.firecracker

# Caddy basic auth (used when GitHub OAuth is not configured)
CADDY_USERNAME=admin
CADDY_PASSWORD=admin

# E2E helper URLs (development defaults)
DASHBOARD_URL=http://localhost:8080
API_URL=http://localhost:8080/api

# GitHub OAuth — control plane (see doc/EDD/003_Caddy_Reverse_Proxy.md appendix)
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
