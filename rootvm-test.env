# E2E test environment for Root VM — stub runtime, ports forwarded from VM to host
# Loaded inside the VM by PM2 via: --env-file=rootvm-test.env
# Also loaded on the host by Playwright via: node --env-file=test.env (for client URLs)

# Server
PORT=9163
SRV0_PORT=9080
SRV1_PORT=9081
SRV2_PORT=9082
DB_PATH=/tmp/rockpool-e2e.db
SPA_ROOT=/mnt/rockpool/packages/client/dist

# Caddy
CADDY_ADMIN_URL=http://localhost:9019
CADDY_USERNAME=test
CADDY_PASSWORD=test

# Queue (ElasticMQ on test port)
QUEUE_ENDPOINT=http://localhost:9424
QUEUE_URL=http://localhost:9424/000000000000/workspace-jobs

# Runtime — stub for E2E testing (use podman when container image is available)
RUNTIME=stub
SSH_KEY_PATH=/mnt/rockpool/images/ssh/rockpool_ed25519

# E2E helper URLs (resolved through Caddy inside the VM)
DASHBOARD_URL=http://localhost:9080
API_URL=http://localhost:9080/api
