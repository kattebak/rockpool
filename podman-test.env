# E2E test environment for Podman workspaces — runs directly on a Linux host
# Loaded by PM2 via: --env-file=podman-test.env
# Also loaded on the host by Playwright via: node --env-file=podman-test.env

# Server
PORT=9163
SRV0_PORT=9080
SRV1_PORT=9081
SRV2_PORT=9082
DB_PATH=/tmp/rockpool-e2e.db
SPA_ROOT=packages/client/dist

# Caddy
CADDY_ADMIN_URL=http://localhost:9019
CADDY_USERNAME=test
CADDY_PASSWORD=test

# Queue (ElasticMQ on test port)
QUEUE_ENDPOINT=http://localhost:9424
QUEUE_URL=http://localhost:9424/000000000000/workspace-jobs

# Runtime — Podman rootless containers
RUNTIME=podman

# E2E helper URLs (resolved through Caddy)
DASHBOARD_URL=http://localhost:9080
API_URL=http://localhost:9080/api
